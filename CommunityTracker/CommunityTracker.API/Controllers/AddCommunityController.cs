using Microsoft.AspNetCore.Mvc;

namespace CommunityTracker.API.Controllers
{
    [ApiController]
    [Route("[controller]")]

    //TODO: MOdify the code for the ADD Community endpoint
    public class AddCommunityController : ControllerBase
    {
        private static readonly string[] Summaries = new[]
        {
        "Freezing", "Bracing", "Chilly", "Cool", "Mild", "Warm", "Balmy", "Hot", "Sweltering", "Scorching"
    };

        private readonly ILogger<AddCommunityController> _logger;

        public AddCommunityController(ILogger<AddCommunityController> logger)
        {
            _logger = logger;
        }

        [HttpGet]
        public IEnumerable<WeatherForecast> Get()
        {
            return Enumerable.Range(1, 5).Select(index => new WeatherForecast
            {
                Date = DateTime.Now.AddDays(index),
                TemperatureC = Random.Shared.Next(-20, 55),
                Summary = Summaries[Random.Shared.Next(Summaries.Length)]
            })
            .ToArray();
        }
    }
}